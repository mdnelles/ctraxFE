import React from "react";
import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles((theme) => ({
   root: {
      display: "flex",
      flexWrap: "wrap",
   },
}));

const Verify = (props) => {
   return <div>this is Verify</div>;
};
export default Verify;
